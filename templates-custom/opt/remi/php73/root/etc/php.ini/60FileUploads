file_uploads                           = On
upload_max_filesize                    = {
    my $mf = $php73{UploadMaxFilesize} || $php{UploadMaxFilesize} || "10M";
    $OUT .= "$mf";
}
; added by stephdl see $php73{MaxFileUpload}
max_file_upload                        = {
    my $mfu = $php73{MaxFileUpload} || $php{MaxFileUpload} || "20";
    $OUT .= "$mfu";
}

file_uploads                           = On
upload_max_filesize                    = {
    my $mf = $php70{UploadMaxFilesize} || $php{UploadMaxFilesize} || "10M";
    $OUT .= "$mf";
}
; added by stephdl see $php70{MaxFileUpload}
max_file_upload                        = {
    my $mfu = $php70{MaxFileUpload} || $php{MaxFileUpload} || "20";
    $OUT .= "$mfu";
}

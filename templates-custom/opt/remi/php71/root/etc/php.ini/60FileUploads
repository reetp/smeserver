file_uploads                           = On
upload_max_filesize                    = {
    my $mf = $php71{UploadMaxFilesize} || $php{UploadMaxFilesize} || "10M";
    $OUT .= "$mf";
}
; added by stephdl see $php71{MaxFileUpload}
max_file_upload                        = {
    my $mfu = $php71{MaxFileUpload} || $php{MaxFileUpload} || "20";
    $OUT .= "$mfu";
}

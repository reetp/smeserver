max_execution_time                     = {
    my $et = $php71{MaxExecutionTime} || $php{MaxExecutionTime} || "30";
    $OUT .= "$et";
}
memory_limit                           = {
    my $ml = $php71{MemoryLimit} || $php{MemoryLimit} || "128M";
    $OUT .= "$ml";
}
;added by stephdl see $php71{MaxInputTime}
max_input_time                     = {
    my $it = $php71{MaxInputTime} || $php{MaxInputTime} || "60";
    $OUT .= "$it";
}

